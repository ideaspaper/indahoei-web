import {Container} from '@/components/container';
import {About as AboutPage} from '@/features/about';

const About = () => {
  return (
    <Container className="mt-16 sm:mt-32">
      <AboutPage />
    </Container>
  );
};

export default About;
